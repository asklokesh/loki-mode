pytest
pytest-asyncio
# The MCP SDK client half (ClientSession/stdio_client), required by
# tests/test-mcp-tool-surface-packaged.sh to handshake the packaged server.
# Same bound as the canonical product constraint in mcp/requirements.txt --
# kept identical there and here so the two cannot drift into disagreeing
# ranges. Not `-r mcp/requirements.txt`: that would also drag chromadb and
# anthropic into every test job, which the shell shards do not need.
mcp>=1.0.0,<2.0.0
pydantic
fastapi
uvicorn
httpx
sqlalchemy[asyncio]
aiosqlite
# Ed25519 receipt signing and JWKS verification. The signing suites print SKIP
# and assert nothing when this module is absent, so an unlisted dependency is a
# silently unmeasured trust core. Same floor as web-app/requirements.txt.
cryptography>=41.0.0
